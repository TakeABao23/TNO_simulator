{
    # Initialization properties
    "first_run": true,
    "date": "autogenerate",
    "time": "autogenerate",
    "user": "autogenerate",
    "TNO_simulator commit hash": "autogenerate",
    "RunGoal" : "Test putting the moon/wide/orbital parameter model directly into runprops.txt (as numpy structured-array draws) instead of a separate params.py module",
    "verbose" : true,

    # Run properties
    "objectname" : "Pluto_test",
    "run_file": "001",
    "reference_pop": "Pluto",		# "Pluto" uses the built-in synthetic test population; otherwise a csv filename (Name,x,y,z,H) found alongside this runprops.txt
    "det_prob_function": "det_prob",	# name of the detection probability function to use from emcee_walker.py

    "nwalkers" : 100,			# number of walkers in emcee ensemble
    "ndim" : 8,				# length of moon_param_names; number of free population-level parameters
    "nburnin" : 50,			# length of burn in
    "nsteps" : 500,			# number of steps for sampling after burn in
    "numpy_seed": null,			# integer for reproducible draws, or null for a time-based seed

    # Moon-like binary parameters: names (fixes the structured-array field
    # order and emcee's flat theta order) and one draw expression per name,
    # each eval'd with `rng` (a numpy Generator) in scope.
    "moon_param_names": ["fb", "ka", "ae", "ke", "ai", "ki", "mdm", "sdm"],
    "moon_fb" : "rng.uniform(0.0, 1.0)",
    "moon_ka" : "rng.uniform(0.1, 5.0)",
    "moon_ae" : "rng.uniform(0.5, 5.0)",
    "moon_ke" : "rng.uniform(0.5, 5.0)",
    "moon_ai" : "rng.uniform(1.9, 2.1)",
    "moon_ki" : "rng.uniform(4.9, 5.1)",
    "moon_mdm" : "rng.uniform(0.0, 4.0)",
    "moon_sdm" : "rng.uniform(0.1, 2.0)",

    # Uniform-prior bounds per moon parameter, used by log_prior to reject
    # e.g. a negative ka/ae/ke/ai/ki before it crashes rng.power/rng.beta.
    # null = unbounded in that direction.
    "param_bounds": {
        "fb":  [0.0, 1.0],
        "ka":  [0.1, null],
        "ae":  [0.5, null],
        "ke":  [0.5, null],
        "ai":  [0.5, null],
        "ki":  [0.5, null],
        "mdm": [null, null],
        "sdm": [0.1, null]
    },

    # Wide-binary parameters -- currently inert (see population_class.py:
    # fb=0 disables the wide-binary branch entirely; ka..sdm are unused
    # placeholders, NaN since a structured float64 array can't hold None).
    "wide_fb" : "0.0",
    "wide_ka" : "np.nan",
    "wide_ae" : "np.nan",
    "wide_ke" : "np.nan",
    "wide_ai" : "np.nan",
    "wide_ki" : "np.nan",
    "wide_mdm" : "np.nan",
    "wide_sdm" : "np.nan",

    # Orbital-element distributions, one zero-arg lambda per name, eval'd
    # with `rng` and `params` (the current moon-level hyperparameter
    # structured record) in scope.
    "orb_param_names": ["a", "e", "i", "w", "Om", "mu"],
    "a" : "lambda: rng.power(params['ka']) * 10000",
    "e" : "lambda: rng.beta(params['ae'], params['ke'])",
    "i" : "lambda: 180.0 * rng.beta(params['ai'], params['ki'])",
    "w" : "lambda: rng.uniform(0.0, 360.0)",
    "Om" : "lambda: rng.uniform(0.0, 360.0)",
    "mu" : "lambda: rng.uniform(0.0, 360.0)",

    # These values are used by the code to manage runs and should not be changed by hand.
    "chain_file": null,			# set by runprops.py when resuming from results/.../chain.h5
    "startfromfile" : false
}
